{# "#align" see penn_entity_preprocess_field #}

{% for item in items %}
  <div class="facts__button {{ item.content['#align'] }}">
    <a href="{{ item.content['#url'] }}" class="btn btn--outline">{{ item.content['#title'] }}</a>
  </div>
{% endfor %}
